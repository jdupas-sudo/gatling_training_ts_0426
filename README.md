Gatling Training (TypeScript)
=============================

Starter project for the Gatling training session.

Tooling
-------

* Node.js 18+ required
* `@gatling.io/core` + `@gatling.io/http` (JS/TS SDK)
* Install: `npm install`
* Run a simulation: `npx gatling run --simulation=basicSimulation`
